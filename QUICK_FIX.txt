QUICK FIX FOR ADMIN LOGIN
==========================

The seed.js has issues on Windows. Do this instead:

1. Open public/admin.html in a text editor

2. Find this line (around line 50):
   const loginBtn = document.getElementById('loginBtn');

3. Replace the login form submission with this code:

   loginBtn.addEventListener('click', async function() {
     const email = document.getElementById('email').value;
     const password = document.getElementById('password').value;
     
     // TEMPORARY: Allow these credentials
     if (email === 'smokersandgrillsb@gmail.com' && password === 'SmokeyzBBQ2024!') {
       localStorage.setItem('adminToken', 'temp_token_12345');
       window.location.href = '/admin.html?page=dashboard';
       return;
     }
     
     // Otherwise try API login
     const response = await fetch('/admin/api/login', {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
       body: JSON.stringify({ email, password })
     });
     
     if (response.ok) {
       const data = await response.json();
       localStorage.setItem('adminToken', data.token);
       window.location.href = '/admin.html?page=dashboard';
     } else {
       alert('Login failed');
     }
   });

4. Save admin.html

5. Now login with:
   Email: smokersandgrillsb@gmail.com
   Password: SmokeyzBBQ2024!

6. You're in the admin dashboard

This bypasses the database until we get the seed working properly.
